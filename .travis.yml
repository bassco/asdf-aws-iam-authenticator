language: c
before_script:
  - git clone https://github.com/asdf-vm/asdf.git asdf
  - . asdf/asdf.sh
script:
  - bin/list-all
  - asdf plugin-add aws-iam-authenticator ./
  - asdf list-all aws-iam-authenticator
  - asdf plugin-test aws-iam-authenticator ./ 'aws-iam-authenticator'

os:
  - linux
  - osx
